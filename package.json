{
  "name": "ccsync",
  "version": "0.0.3",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "test": "bun test ./tests",
    "build": "bun build src/index.ts --outdir ./dist --target bun",
    "build-ex": "bun build src/index.ts --outfile ./dist/cli --target bun --compile"
  },
  "devDependencies": {
    "@types/bun": "^1.2.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@clack/prompts": "^0.10.0",
    "chokidar": "^4.0.3",
    "glob": "^11.0.1",
    "picocolors": "^1.1.1",
    "ts-deepmerge": "^7.0.2",
    "yaml": "^2.7.0",
    "zod": "^3.24.2"
  }
}